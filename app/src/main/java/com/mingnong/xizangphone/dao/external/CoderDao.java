package com.mingnong.xizangphone.dao.external;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import com.mingnong.xizangphone.dao.Coder;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.annotation.Keep;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "Coder".
*/
@Keep
public class CoderDao extends AbstractDao<Coder, Long> {

    public static final String TABLENAME = "Coder";

    /**
     * Properties of entity Coder.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property CId = new Property(0, Long.class, "cId", true, "CId");
        public final static Property CParent = new Property(1, String.class, "cParent", false, "FCPARENT");
        public final static Property CName = new Property(2, String.class, "cName", false, "FcName");
        public final static Property CdIsPlay = new Property(3, String.class, "cdIsPlay", false, "cdIsPlay");
        public final static Property CDelete = new Property(4, String.class, "cDelete", false, "cDelete");
        public final static Property Corder = new Property(5, String.class, "corder", false, "corder");
        public final static Property CValue = new Property(6, String.class, "cValue", false, "cValue");
    }


    public CoderDao(DaoConfig config) {
        super(config);
    }
    
    public CoderDao(DaoConfig config, DaoSessionExternal daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"Coder\" (" + //
                "\"CId\" INTEGER PRIMARY KEY ," + // 0: cId
                "\"FCPARENT\" TEXT," + // 1: cParent
                "\"FcName\" TEXT," + // 2: cName
                "\"cdIsPlay\" TEXT," + // 3: cdIsPlay
                "\"cDelete\" TEXT," + // 4: cDelete
                "\"corder\" TEXT," + // 5: corder
                "\"cValue\" TEXT);"); // 6: cValue
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"Coder\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Coder entity) {
        stmt.clearBindings();
 
        Long cId = entity.getCId();
        if (cId != null) {
            stmt.bindLong(1, cId);
        }
 
        String cParent = entity.getCParent();
        if (cParent != null) {
            stmt.bindString(2, cParent);
        }
 
        String cName = entity.getCName();
        if (cName != null) {
            stmt.bindString(3, cName);
        }
 
        String cdIsPlay = entity.getCdIsPlay();
        if (cdIsPlay != null) {
            stmt.bindString(4, cdIsPlay);
        }
 
        String cDelete = entity.getCDelete();
        if (cDelete != null) {
            stmt.bindString(5, cDelete);
        }
 
        String corder = entity.getCorder();
        if (corder != null) {
            stmt.bindString(6, corder);
        }
 
        String cValue = entity.getCValue();
        if (cValue != null) {
            stmt.bindString(7, cValue);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Coder entity) {
        stmt.clearBindings();
 
        Long cId = entity.getCId();
        if (cId != null) {
            stmt.bindLong(1, cId);
        }
 
        String cParent = entity.getCParent();
        if (cParent != null) {
            stmt.bindString(2, cParent);
        }
 
        String cName = entity.getCName();
        if (cName != null) {
            stmt.bindString(3, cName);
        }
 
        String cdIsPlay = entity.getCdIsPlay();
        if (cdIsPlay != null) {
            stmt.bindString(4, cdIsPlay);
        }
 
        String cDelete = entity.getCDelete();
        if (cDelete != null) {
            stmt.bindString(5, cDelete);
        }
 
        String corder = entity.getCorder();
        if (corder != null) {
            stmt.bindString(6, corder);
        }
 
        String cValue = entity.getCValue();
        if (cValue != null) {
            stmt.bindString(7, cValue);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Coder readEntity(Cursor cursor, int offset) {
        Coder entity = new Coder( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // cId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // cParent
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // cName
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // cdIsPlay
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // cDelete
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // corder
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6) // cValue
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Coder entity, int offset) {
        entity.setCId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCParent(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setCName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCdIsPlay(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setCDelete(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setCorder(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setCValue(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Coder entity, long rowId) {
        entity.setCId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Coder entity) {
        if(entity != null) {
            return entity.getCId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Coder entity) {
        return entity.getCId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
