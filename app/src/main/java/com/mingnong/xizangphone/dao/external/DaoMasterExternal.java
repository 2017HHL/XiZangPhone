package com.mingnong.xizangphone.dao.external;

import android.content.Context;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteDatabase.CursorFactory;
import android.util.Log;

import org.greenrobot.greendao.AbstractDaoMaster;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseOpenHelper;
import org.greenrobot.greendao.database.StandardDatabase;
import org.greenrobot.greendao.identityscope.IdentityScopeType;


// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * Master of DAO (schema version 11): knows all DAOs.
 */
public class DaoMasterExternal extends AbstractDaoMaster {
    public static final int SCHEMA_VERSION = 11;

    /** Creates underlying database table using DAOs. */
    public static void createAllTables(Database db, boolean ifNotExists) {
//        AnimalApplyBeanDao.createTable(db, ifNotExists);
//        Animal_ADao.createTable(db, ifNotExists);
//        Animal_BDao.createTable(db, ifNotExists);
//        CacheTableDao.createTable(db, ifNotExists);
//        ProductApplyBeanDao.createTable(db, ifNotExists);
//        Product_ADao.createTable(db, ifNotExists);
//        Product_BDao.createTable(db, ifNotExists);
//        UserDao.createTable(db, ifNotExists);
        CodeAnimalDao.createTable(db, ifNotExists);
        CodeProductDao.createTable(db, ifNotExists);
        CoderDao.createTable(db, ifNotExists);
        PrinterDao.createTable(db, ifNotExists);
//        UnitDao.createTable(db, ifNotExists);
    }

    /** Drops underlying database table using DAOs. */
    public static void dropAllTables(Database db, boolean ifExists) {
//        AnimalApplyBeanDao.dropTable(db, ifExists);
//        Animal_ADao.dropTable(db, ifExists);
//        Animal_BDao.dropTable(db, ifExists);
//        CacheTableDao.dropTable(db, ifExists);
//        ProductApplyBeanDao.dropTable(db, ifExists);
//        Product_ADao.dropTable(db, ifExists);
//        Product_BDao.dropTable(db, ifExists);
//        UserDao.dropTable(db, ifExists);
        CodeAnimalDao.dropTable(db, ifExists);
//        UnitDao.dropTable(db, ifExists);
        CoderDao.dropTable(db, ifExists);
        CodeProductDao.dropTable(db, ifExists);
        PrinterDao.dropTable(db, ifExists);
    }

    /**
     * WARNING: Drops all table on Upgrade! Use only during development.
     * Convenience method using a {@link DevOpenHelper}.
     */
    public static DaoSessionExternal newDevSession(Context context, String name) {
        Database db = new DevOpenHelper(context, name).getWritableDb();
        DaoMasterExternal daoMaster = new DaoMasterExternal(db);
        return daoMaster.newSession();
    }

    public DaoMasterExternal(SQLiteDatabase db) {
        this(new StandardDatabase(db));
    }

    public DaoMasterExternal(Database db) {
        super(db, SCHEMA_VERSION);
//        registerDaoClass(AnimalApplyBeanDao.class);
//        registerDaoClass(Animal_ADao.class);
//        registerDaoClass(Animal_BDao.class);
//        registerDaoClass(CacheTableDao.class);
        registerDaoClass(CodeAnimalDao.class);
        registerDaoClass(CodeProductDao.class);
        registerDaoClass(CoderDao.class);
        registerDaoClass(PrinterDao.class);
//        registerDaoClass(UnitDao.class);
//        registerDaoClass(ProductApplyBeanDao.class);
//        registerDaoClass(Product_ADao.class);
//        registerDaoClass(UserDao.class);
//        registerDaoClass(Product_BDao.class);
    }

    public DaoSessionExternal newSession() {
        return new DaoSessionExternal(db, IdentityScopeType.Session, daoConfigMap);
    }

    public DaoSessionExternal newSession(IdentityScopeType type) {
        return new DaoSessionExternal(db, type, daoConfigMap);
    }

    /**
     * Calls {@link #createAllTables(Database, boolean)} in {@link #onCreate(Database)} -
     */
    public static abstract class OpenHelper extends DatabaseOpenHelper {
        public OpenHelper(Context context, String name) {
            super(context, name, SCHEMA_VERSION);
        }

        public OpenHelper(Context context, String name, CursorFactory factory) {
            super(context, name, factory, SCHEMA_VERSION);
        }

        @Override
        public void onCreate(Database db) {
            Log.i("greenDAO", "Creating tables for schema version " + SCHEMA_VERSION);
            createAllTables(db, true);
            db.beginTransaction();

            db.endTransaction();
        }
    }

    /** WARNING: Drops all table on Upgrade! Use only during development. */
    public static class DevOpenHelper extends OpenHelper {
        public DevOpenHelper(Context context, String name) {
            super(context, name);
        }

        public DevOpenHelper(Context context, String name, CursorFactory factory) {
            super(context, name, factory);
        }


        @Override
        public void onUpgrade(Database db, int oldVersion, int newVersion) {
            Log.i("greenDAO", "Upgrading schema from version " + oldVersion + " to " + newVersion + " by dropping all tables");
            dropAllTables(db, true);
            onCreate(db);
        }
    }

}
